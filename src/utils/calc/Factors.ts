// export class Factors {
//   constructor(private factorToPowerMap: Record<number, number>) {}
//   getValues = () =>
//     Object.entries(this.factorToPowerMap).reduce(
//       (product, [factor, power]) => product * parseInt(factor) ** power,
//       1
//     );
//   lcm =
// }
