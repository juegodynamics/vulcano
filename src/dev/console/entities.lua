/c
entities = {}
for k, v in pairs(game.entity_prototypes) do
  table.insert(entities, serpent.line({
    name=v.name, 
    type=v.type,
    localised_description=v.localised_description,
    localised_name=v.localised_name,
    order=v.order,
    max_health=v.max_health,
    infinite_resource=v.infinite_resource,
    minimum_resource_amount=v.minimum_resource_amount,
    normal_resource_amount=v.normal_resource_amount,
    infinite_depletion_resource_amount=v.infinite_depletion_resource_amount,
    resource_category=v.resource_category,
    mineable_properties=v.mineable_properties,
    items_to_place_this=v.items_to_place_this,
    collision_box=v.collision_box,
    secondary_collision_box=v.secondary_collision_box,
    map_generator_bounding_box=v.map_generator_bounding_box,
    selection_box=v.selection_box,
    drawing_box=v.drawing_box,
    sticker_box=v.sticker_box,
    collision_mask=v.collision_mask,
    collision_mask_with_flags=v.collision_mask_with_flags,
    order=v.order,
    group=v.group,
    subgroup=v.subgroup,
    healing_per_tick=v.healing_per_tick,
    emissions_per_second=v.emissions_per_second,
    corpses=v.corpses,
    selectable_in_game=v.selectable_in_game,
    selection_priority=v.selection_priority,
    weight=v.weight,
    resistances=v.resistances,
    fast_replaceable_group=v.fast_replaceable_group,
    next_upgrade=v.next_upgrade,
    loot=v.loot,
    repair_speed_modifier=v.repair_speed_modifier,
    turret_range=v.turret_range,
    autoplace_specification=v.autoplace_specification,
    belt_speed=v.belt_speed,
    result_units=v.result_units,
    attack_result=v.attack_result,
    final_attack_result=v.final_attack_result,
    attack_parameters=v.attack_parameters,
    spawn_cooldown=v.spawn_cooldown,
    mining_drill_radius=v.mining_drill_radius,
    mining_speed=v.mining_speed,
    logistic_mode=v.logistic_mode,
    max_underground_distance=v.max_underground_distance,
    flags=v.flags,
    remains_when_mined=v.remains_when_mined,
    additional_pastable_entities=v.additional_pastable_entities,
    allow_copy_paste=v.allow_copy_paste,
    shooting_cursor_size=v.shooting_cursor_size,
    created_smoke=v.created_smoke,
    created_effect=v.created_effect,
    map_color=v.map_color,
    friendly_map_color=v.friendly_map_color,
    enemy_map_color=v.enemy_map_color,
    build_base_evolution_requirement=v.build_base_evolution_requirement,
    instruments=v.instruments,
    max_polyphony=v.max_polyphony,
    module_inventory_size=v.module_inventory_size,
    ingredient_count=v.ingredient_count,
    crafting_speed=v.crafting_speed,
    crafting_categories=v.crafting_categories,
    resource_categories=v.resource_categories,
    supply_area_distance=v.supply_area_distance,
    max_wire_distance=v.max_wire_distance,
    max_circuit_wire_distance=v.max_circuit_wire_distance,
    energy_usage=v.energy_usage,
    max_energy_usage=v.max_energy_usage,
    effectivity=v.effectivity,
    consumption=v.consumption,
    friction_force=v.friction_force,
    braking_force=v.braking_force,
    tank_driving=v.tank_driving,
    rotation_speed=v.rotation_speed,
    turret_rotation_speed=v.turret_rotation_speed,
    guns=v.guns,
    speed=v.speed,
    speed_multiplier_when_out_of_energy=v.speed_multiplier_when_out_of_energy,
    max_payload_size=v.max_payload_size,
    draw_cargo=v.draw_cargo,
    energy_per_move=v.energy_per_move,
    energy_per_tick=v.energy_per_tick,
    max_energy=v.max_energy,
    min_to_charge=v.min_to_charge,
    max_to_charge=v.max_to_charge,
    burner_prototype=v.burner_prototype,
    electric_energy_source_prototype=v.electric_energy_source_prototype,
    heat_energy_source_prototype=v.heat_energy_source_prototype,
    fluid_energy_source_prototype=v.fluid_energy_source_prototype,
    void_energy_source_prototype=v.void_energy_source_prototype,
    building_grid_bit_shift=v.building_grid_bit_shift,
    fluid_usage_per_tick=v.fluid_usage_per_tick,
    maximum_temperature=v.maximum_temperature,
    target_temperature=v.target_temperature,
    fluid=v.fluid,
    fluid_capacity=v.fluid_capacity,
    pumping_speed=v.pumping_speed,
    stack=v.stack,
    allow_custom_vectors=v.allow_custom_vectors,
    allow_burner_leech=v.allow_burner_leech,
    inserter_extension_speed=v.inserter_extension_speed,
    inserter_rotation_speed=v.inserter_rotation_speed,
    inserter_pickup_position=v.inserter_pickup_position,
    inserter_drop_position=v.inserter_drop_position,
    inserter_chases_belt_items=v.inserter_chases_belt_items,
    count_as_rock_for_filtered_deconstruction=v.count_as_rock_for_filtered_deconstruction,
    filter_count=v.filter_count,
    production=v.production,
    time_to_live=v.time_to_live,
    distribution_effectivity=v.distribution_effectivity,
    explosion_beam=v.explosion_beam,
    explosion_rotate=v.explosion_rotate,
    tree_color_count=v.tree_color_count,
    alert_when_damaged=v.alert_when_damaged,
    alert_when_attacking=v.alert_when_attacking,
    color=v.color,
    collision_mask_collides_with_self=v.collision_mask_collides_with_self,
    collision_mask_collides_with_tiles_only=v.collision_mask_collides_with_tiles_only,
    collision_mask_considers_tile_transitions=v.collision_mask_considers_tile_transitions,
    allowed_effects=v.allowed_effects,
    rocket_parts_required=v.rocket_parts_required,
    fixed_recipe=v.fixed_recipe,
    construction_radius=v.construction_radius,
    logistic_radius=v.logistic_radius,
    energy_per_hit_point=v.energy_per_hit_point,
    create_ghost_on_death=v.create_ghost_on_death,
    timeout=v.timeout,
    fluidbox_prototypes=v.fluidbox_prototypes,
    neighbour_bonus=v.neighbour_bonus,
    neighbour_collision_increase=v.neighbour_collision_increase,
    container_distance=v.container_distance,
    belt_distance=v.belt_distance,
    belt_length=v.belt_length,
    is_building=v.is_building,
    automated_ammo_count=v.automated_ammo_count,
    max_speed=v.max_speed,
    darkness_for_all_lamps_on=v.darkness_for_all_lamps_on,
    darkness_for_all_lamps_off=v.darkness_for_all_lamps_off,
    always_on=v.always_on,
    min_darkness_to_spawn=v.min_darkness_to_spawn,
    max_darkness_to_spawn=v.max_darkness_to_spawn,
    call_for_help_radius=v.call_for_help_radius,
    max_count_of_owned_units=v.max_count_of_owned_units,
    max_friends_around_to_spawn=v.max_friends_around_to_spawn,
    spawning_radius=v.spawning_radius,
    spawning_spacing=v.spawning_spacing,
    radius=v.radius,
    cliff_explosive_prototype=v.cliff_explosive_prototype,
    rocket_entity_prototype=v.rocket_entity_prototype,
    has_belt_immunity=v.has_belt_immunity,
    vision_distance=v.vision_distance,
    pollution_to_join_attack=v.pollution_to_join_attack,
    min_pursue_time=v.min_pursue_time,
    max_pursue_distance=v.max_pursue_distance,
    radar_range=v.radar_range,
    move_while_shooting=v.move_while_shooting,
    can_open_gates=v.can_open_gates,
    affected_by_tiles=v.affected_by_tiles,
    distraction_cooldown=v.distraction_cooldown,
    spawning_time_modifier=v.spawning_time_modifier,
    alert_icon_shift=v.alert_icon_shift,
    lab_inputs=v.lab_inputs,
    researching_speed=v.researching_speed,
    item_slot_count=v.item_slot_count,
    base_productivity=v.base_productivity,
    allow_access_to_all_forces=v.allow_access_to_all_forces,
    supports_direction=v.supports_direction,
    terrain_friction_modifier=v.terrain_friction_modifier,
    max_distance_of_sector_revealed=v.max_distance_of_sector_revealed,
    max_distance_of_nearby_sector_revealed=v.max_distance_of_nearby_sector_revealed,
    adjacent_tile_collision_box=v.adjacent_tile_collision_box,
    adjacent_tile_collision_mask=v.adjacent_tile_collision_mask,
    adjacent_tile_collision_test=v.adjacent_tile_collision_test,
    center_collision_mask=v.center_collision_mask,
    grid_prototype=v.grid_prototype,
    remove_decoratives=v.remove_decoratives,
    running_speed=v.running_speed,
    maximum_corner_sliding_distance=v.maximum_corner_sliding_distance,
    build_distance=v.build_distance,
    drop_item_distance=v.drop_item_distance,
    reach_distance=v.reach_distance,
    reach_resource_distance=v.reach_resource_distance,
    item_pickup_distance=v.item_pickup_distance,
    loot_pickup_distance=v.loot_pickup_distance,
    enter_vehicle_distance=v.enter_vehicle_distance,
    ticks_to_keep_gun=v.ticks_to_keep_gun,
    ticks_to_keep_aiming_direction=v.ticks_to_keep_aiming_direction,
    ticks_to_stay_in_combat=v.ticks_to_stay_in_combat,
    respawn_time=v.respawn_time,
    damage_hit_tint=v.damage_hit_tint,
    character_corpse=v.character_corpse,
    valid=v.valid,
  }))
end
game.player.print(table.concat(entities, "\n"))
game.write_file("entities.lua.stream", table.concat(entities, "\n"))